services:
  - type: web
    name: soi-agent
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: AI_INTEGRATIONS_GLM_API_KEY
        sync: false
